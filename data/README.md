# data module (coming soon)

Will contain commands to download historical candles from Zerodha, e.g.

    python main.py data --symbol NIFTY --interval 5minute --days 30

Downloaded CSV files are saved in `data/DataBank/`.
